{
  "name": "@reallyartificial/freeport",
  "version": "1.0.1",
  "description": "Open-source, self-hosted, OpenAI-compatible LLM gateway — multi-provider routing, fallback, semantic cache, budget caps, PII guardrails, audit logs",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "freeport": "bin/freeport.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "bin",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "setup": "npm install && npm run build:ui && echo '\n  Ready! Run: npm run dev\n'",
    "build": "tsc && cp src/db/migrations/*.sql dist/db/migrations/",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build:ui": "cd admin-ui && npm install --no-audit --no-fund && npm run build",
    "copy:ui": "node scripts/copy-ui.js",
    "prepack": "npm run build && npm run build:ui && npm run copy:ui",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@fastify/helmet": "^13.0.2",
    "@fastify/static": "^8.1.0",
    "ajv": "^8.17.1",
    "better-sqlite3": "^11.8.1",
    "fastify": "^5.2.1",
    "pino": "^9.6.0",
    "yaml": "^2.7.0"
  },
  "optionalDependencies": {
    "@huggingface/transformers": "^3.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.12.0",
    "eslint": "^9.18.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.21.0",
    "vitest": "^4.1.2"
  }
}
